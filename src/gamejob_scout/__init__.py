"""GameJob Scout: human-in-the-loop job discovery for game studios and tech companies.

This package is deliberately thin right now. Functionality is added milestone by
milestone (collectors, normalization, persistence, CLI) rather than all at once.
"""

__version__ = "0.1.0"

__all__ = ["__version__"]
